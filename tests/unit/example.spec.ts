/**
 * Unit test template for Rotagotchi.
 *
 * USAGE: Copy this file to tests/unit/<your-module>.spec.ts to add unit tests
 * for a new module. Unit tests run in Node.js — no browser, no extension
 * context. Test pure functions and business logic here.
 *
 * All files matching tests/unit/**\/*.spec.ts are auto-discovered by Vitest.
 * No config changes needed when adding new test files.
 *
 * Example imports:
 *   import { myFunction } from '../../extension/my-module';
 */
import { describe, it, expect } from 'vitest';

describe('example', () => {
  it('placeholder — replace with real assertions', () => {
    expect(true).toBe(true);
  });
});
