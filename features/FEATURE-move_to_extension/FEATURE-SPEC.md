# Feature Spec -> Moving the current logic to the extension

## Requirements
- move the lottie animation logic to the extension
- use the extension to return the currently active tab url
- determine if the currently active URL is part of the whitelist or not
- change animations and state based on if whitelist is active
    - for now only add health when whitelist is active, report health on the extensions UI
