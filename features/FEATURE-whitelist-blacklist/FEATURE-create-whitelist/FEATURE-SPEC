# Feature -> {Unproductive Content Whitelist}

---

## Requirements

---

- Create a JSON-based whitelist.json that stores domains classified as "unproductive/degenerative" (e.g youtube.com., instagram.com)
- Write a utility function isUnproductive(url) that checks if a given URL is in that list.
- Ensure the matching logic handles subdomains (e.g., m.youtube.com should be considereed youtube.com)

After the above run the following with this file and constitution.md loaded into AI context:

> Using this basic list of requirements, ask me questions to clear up ANY ambiguity. With those, then rewrite the requirements to have greater depth reread constitution to make sure you understand project goals as well. Add these new requirements to the md file.
> ^^ Review these
> Create an implementation plan like the below
> **_{Checkpoint name}_**
>
> - [ ] what needs to get done
> - [ ] what needs to get done
> - [ ] {a test here for that checkpoint}
>
> **_{Checkpoint name}_**
>
> - [ ] what needs to get done
> - [ ] what needs to get done
> - [ ] {a test here for that checkpoint}
>
> Where checkpoints are shorter parts (variable in length) that can be their own commits with tests for each basic part of functionality. Make sure to follow the technical structure of this project as well. Add this to the document.
